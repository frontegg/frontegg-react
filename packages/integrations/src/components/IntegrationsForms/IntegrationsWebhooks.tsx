import React, { FC } from 'react';
import { IIntegrationsComponent } from '../../interfaces';

export const IntegrationsWebhooks: FC<IIntegrationsComponent> = ({ onClose }) => {
  return <div>Webhooks</div>;
};
