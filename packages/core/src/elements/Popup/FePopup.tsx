import { PopupProps } from './interfaces';
import React, { forwardRef } from 'react';

export const FePopup = forwardRef<HTMLDivElement, PopupProps>((props, ref) => {
  return <div ref={ref}> NOT IMPLEMENTED</div>;
});
